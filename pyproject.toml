[project]
name = "patient-condition-classifier"
version = "0.1.0"
description = "Preprocesses the UCI Drug Reviews dataset for patient condition classification using DeBERTa-v3"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "datasets>=5.0.0",
    "transformers>=5.14.0",
]
